# E-Retail-Platform_Class-12
This Python-based project, built with the CustomTkinter library for the user interface (UI) and MySQL as the backend database, aims to simulate a model application of an e-retail platform.
